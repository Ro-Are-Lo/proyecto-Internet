// SERVICIO
// Aquí van las funciones que hablan con APIs, base de datos, sockets, etc.
// Encapsulan la lógica de acceso a datos, separada de la UI.

import { User } from "../models/User";

export async function fetchUsers(): Promise<User[]> {
  // Simulación: Traemos usuarios desde una API pública
  const res = await fetch("https://jsonplaceholder.typicode.com/users");
  return await res.json();
}